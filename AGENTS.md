# UIComposable Agent Instructions

## Policy Bootstrap

The AI working policies for this repository are in [UIComposable Agent Policy](https://app.notion.com/p/3e0b88a8aa548135b808d98afc549847). Repository code, tests, package configuration, CI/CD, and GitHub templates are the source of implementation facts.

Before acting, consult only policies marked `Active` in the Policy Index.

1. Read `General` and `UIComposable` for every task.
2. Read each additional policy that matches the current request.
3. Do not read policies absent from the Policy Index, `Inactive` policies, or unrelated Decision Memory and Specs.
4. Stop and report the unavailable policy when a required Notion policy cannot be loaded.

Notion owns policy content and Agent Contracts. Do not duplicate policy content in this file.
