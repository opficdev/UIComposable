name = "code_reviewer"
description = "Reviews explicitly requested code changes and final SDD diffs"
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Use only when the user requests review or an explicit SDD workflow reaches final review.
Compare the diff with the Issue or Spec acceptance criteria and prioritize correctness, regressions, public API stability, concurrency, and missing verification.
Report only evidenced findings and do not modify code.
"""
